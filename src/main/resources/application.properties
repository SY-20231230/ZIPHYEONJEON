# =====================================================================
# [집현전 프로젝트] 통합 환경 설정 (Standardized for Team - Fix Ver.)
# =====================================================================

# 1. 기본 프로젝트 정보
spring.application.name=ZIPHYEONJEON
spring.profiles.include=local

# 2. 클라우드 및 보안 기본 설정 (팀원 환경 유지)
spring.cloud.config.enabled=false
# spring.config.import=optional:configserver:http://localhost:8888
spring.security.user.name=root
spring.security.user.password=1234

# 3. 데이터베이스 (MySQL)
# 💡 핵심 수정: 동규님의 실제 비밀번호를 기본값으로 설정하되, 환경변수가 있다면 우선시합니다.
spring.datasource.url=${DB_URL:jdbc:mysql://ziphyeonjeon.czs28cg68sew.ap-northeast-2.rds.amazonaws.com:3306/ZIPHYEONJEON}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:AX9b8XwT8jQNLYX}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 4. JPA/Hibernate 설정 - 팀원 명명 전략 보존
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.default_batch_fetch_size=100

# 5. Redis 설정 - 동규님의 RTR 보안 엔진 필수 항목
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# 6. 앱 자체 보안 및 JWT 설정 - 동규님 코드 연동
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}
jwt.secret=${JWT_SECRET:vmfhaltjskstskswhadsregnalroqkfwkdbalroqkfwkdbalroqkfwkdbal}
app.jwt.access-token-validity=3600000
app.jwt.refresh-token-validity=1209600000

# 7. 로깅 및 서버 인코딩 설정
server.port=8080
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

logging.level.root=INFO
logging.level.io.pjj.ziphyeonjeon=INFO
logging.level.org.hibernate.SQL=WARN
logging.file.name=${LOG_PATH:./logs}/ziphyeonjeon.log

# =====================================================================
# [집현전 프로젝트] 외부 API 임시 키 세트 (부팅 에러 방어용)
# =====================================================================

# 1. 공공데이터포털 관련 (부동산 실거래가, 재난 안전 데이터)
PUBLIC_DATA_SERVICE_KEY=temp_data_key
DISASTER_SERVICE_KEY=temp_disaster_key

# 2. 네이버 클라우드 OCR 관련 (현재 발생한 에러 해결용)
NAVER_OCR_SERVICE_KEY=temp_ocr_key
NAVER_APIGW_INVOKE_URL=https://api.naver.com/temp
NAVER_APIGW_API_KEY_ID=temp_id
NAVER_APIGW_API_KEY=temp_key

# 3. 기타 지도 및 검색 API (예상되는 다음 에러 방어용)
VWORLD_API_KEY=29A7DBF2-E9FF-3239-995A-60AF058FD2B6
SEOUL_OPENAPI_KEY=5a78574d4279737933374e584b6473
KAKAO_API_KEY=temp_kakao_key